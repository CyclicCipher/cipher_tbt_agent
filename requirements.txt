# Cipher's TBT Agent — dependencies.
# The demos run on CPU; a GPU is optional (everything fits in 4 GB VRAM).
# For CUDA, install the torch build matching your CUDA version (see https://pytorch.org).
torch>=2.0
numpy>=1.24
scipy>=1.10
