"""ARC-AGI-3 replica agents.

  column/  — the TBT cortical-column agent (the active line: control loop, learned dynamics, recurrence).
  wm/      — the predecessor symbolic world-model agent (perceive→induce→infer-goal→plan), kept as a reference.
  layouts.py — procedural LockPath layout generator (used to make harder boards).
"""
